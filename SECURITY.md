# سیاست امنیتی Daymark

## دامنه

این سیاست برای سورس Android پروژه Daymark و فایل‌های منتشرشده در GitHub Releases است.

## گزارش آسیب‌پذیری

آسیب‌پذیری‌های امنیتی را به‌صورت عمومی همراه با داده خصوصی کاربر منتشر نکنید. برای پروژه دانشگاهی فعلی، گزارش باید مستقیماً به مالک Repository یا مسئول معماری تیم تحویل شود و شامل موارد زیر باشد:

- نسخه یا Commit آسیب‌پذیر
- شرح اثر
- مراحل بازتولید حداقلی
- Log پاک‌سازی‌شده
- پیشنهاد اصلاح

## کنترل‌های فعلی

- برنامه حساب کاربری و Backend ندارد.
- داده اصلی در SQLite محلی ذخیره می‌شود.
- Queryها باید پارامتری باشند.
- Transactionها برای عملیات چندمرحله‌ای استفاده می‌شوند.
- فایل Database، Log، APK، Keystore و Secret توسط `.gitignore` از Repository حذف می‌شوند.
- CI تست، Coverage، Compile و بررسی‌های امنیتی وابستگی را اجرا می‌کند.

## محدودیت‌های شناخته‌شده

- APK دانشگاهی فعلی Debug Build است و Production-signed نیست.
- Dependency Audit به پایگاه داده آنلاین آسیب‌پذیری وابسته است.
- نبود Backend ریسک‌های Server-side را حذف می‌کند، اما امنیت دستگاه و Backup سیستم‌عامل خارج از کنترل برنامه است.
- تست نفوذ رسمی مستقل انجام نشده است.

## نسخه‌های پشتیبانی‌شده

| نسخه | وضعیت |
|---|---|
| آخرین Release دانشگاهی | بررسی و اصلاح در صورت گزارش معتبر |
| Buildهای قدیمی | فقط در صورت امکان بازتولید |
